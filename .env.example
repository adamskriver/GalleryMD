# Example .env for GitHub Actions self-hosted runner
# Copy this to .env and run scripts/get-runner-token.ps1 to populate RUNNER_TOKEN

# Repository in format: owner/repo
REPO=adamskriver/GalleryMD

# Runner configuration
RUNNER_NAME=gallerymd-runner
LABELS=self-hosted,docker,windows
RUNNER_WORKDIR=/tmp/runner
RUNNER_SCOPE=repo

# RUNNER_TOKEN will be populated by scripts/get-runner-token.ps1
# Do not manually set this; the script fetches a fresh token from GitHub

# Application Configuration
# Path on host machine to mount as /docs in container (for case studies)
# Use forward slashes even on Windows (Docker handles conversion)
# Example: Z:/Nineriver Technologies/Dev
MD_ROOT_HOST=./docs

# Admin token for API authentication (generate a secure random string)
# Example: openssl rand -base64 32
ADMIN_TOKEN=your-admin-token-here
